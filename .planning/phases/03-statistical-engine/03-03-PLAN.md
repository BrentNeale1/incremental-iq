---
phase: 03-statistical-engine
plan: 03
type: tdd
wave: 2
depends_on: [03-02]
files_modified:
  - packages/analysis/models/baseline.py
  - packages/analysis/routers/forecast.py
  - packages/analysis/tests/test_forecast.py
autonomous: true
requirements: [STAT-01, STAT-03, STAT-07, SEAS-01]

must_haves:
  truths:
    - "System builds a baseline forecast from historical campaign data using Prophet"
    - "Forecast includes confidence intervals (yhat_lower, yhat_upper) not just point estimates"
    - "Retail event calendar is injected as Prophet holidays, adjusting the baseline for known seasonal spikes"
    - "User-defined brand events are accepted and merged into the holiday calendar"
    - "Zero-spend days are filtered before fitting to avoid nonsensical weekly patterns"
  artifacts:
    - path: "packages/analysis/models/baseline.py"
      provides: "Prophet model wrapper with holiday injection and zero-spend filtering"
      contains: "fit_baseline"
    - path: "packages/analysis/routers/forecast.py"
      provides: "POST /forecast endpoint accepting ForecastRequest, returning ForecastResponse"
      contains: "router.post"
    - path: "packages/analysis/tests/test_forecast.py"
      provides: "Tests for baseline model and forecast endpoint"
      contains: "test_forecast"
  key_links:
    - from: "packages/analysis/routers/forecast.py"
      to: "packages/analysis/models/baseline.py"
      via: "import fit_baseline"
      pattern: "from models.baseline import"
    - from: "packages/analysis/models/baseline.py"
      to: "packages/analysis/data/retail_calendar.py"
      via: "import get_retail_events"
      pattern: "from data.retail_calendar import"
    - from: "packages/analysis/main.py"
      to: "packages/analysis/routers/forecast.py"
      via: "include_router"
      pattern: "app\\.include_router.*forecast"
---

<objective>
Implement the Prophet baseline forecasting model and /forecast FastAPI endpoint using TDD. The model fits a per-campaign time series with retail event calendar holidays, produces a 90-day forward forecast with confidence intervals, and decomposes the signal into trend, seasonal, and holiday components.

Purpose: Baseline forecasting is the foundation for counterfactual estimation — "what would have happened without the campaign change." STAT-01 requires per-campaign models; STAT-03 requires confidence intervals; STAT-07 requires the model to improve with more data (Prophet naturally does this with more observations). SEAS-01 integration ensures seasonal events are accounted for in the baseline.
Output: Working /forecast endpoint with Prophet model, tested with synthetic data.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-statistical-engine/03-RESEARCH.md
@.planning/phases/03-statistical-engine/03-02-SUMMARY.md
@packages/analysis/schemas/requests.py
@packages/analysis/schemas/responses.py
@packages/analysis/data/retail_calendar.py
@packages/analysis/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Write failing tests for baseline forecasting</name>
  <files>
    packages/analysis/tests/__init__.py
    packages/analysis/tests/test_forecast.py
  </files>
  <action>
Create test file with the following test cases using pytest + httpx (FastAPI TestClient):

1. **test_fit_baseline_returns_forecast** — Generate 180 days of synthetic revenue data (sine wave + trend + noise to simulate seasonality), call `fit_baseline()` directly, assert: returns dict with 'forecast', 'components' keys; forecast list has length > 180 (original + future); each forecast point has 'ds', 'yhat', 'yhat_lower', 'yhat_upper'.

2. **test_forecast_includes_confidence_intervals** — Same synthetic data, assert yhat_lower < yhat < yhat_upper for each forecast point (STAT-03).

3. **test_forecast_incorporates_holidays** — Generate 2 years of data with a clear spike at Christmas dates. Fit with holidays enabled, assert holiday component is non-zero around Dec 25 dates.

4. **test_forecast_filters_zero_spend_days** — Generate data with 30% zero-spend days randomly inserted. Call fit_baseline with the full dataset. Assert it does not crash and returns a valid forecast (Pitfall 2 mitigation).

5. **test_forecast_endpoint_returns_200** — Use FastAPI TestClient to POST /forecast with valid ForecastRequest body (180 days of synthetic data). Assert status 200, response parses as ForecastResponse.

6. **test_forecast_endpoint_rejects_insufficient_data** — POST /forecast with only 10 days of data. Assert status 422 or 400 with error message indicating insufficient data.

Generate synthetic data helper: `def make_synthetic_revenue(days: int, trend: float = 0.5, noise: float = 10.0) -> list[dict]` returning list of {date, spend_usd, revenue, conversions} dicts.

Tests MUST fail initially (models/baseline.py and routers/forecast.py don't exist yet).
  </action>
  <verify>
    <automated>cd C:/Users/brent/incremental-iq/packages/analysis && python -m pytest tests/test_forecast.py -x --tb=short 2>&1 | head -20</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Tests exist and fail with ImportError or ModuleNotFoundError (RED phase confirmed)</done>
</task>

<task type="auto">
  <name>Task 2: GREEN + REFACTOR — Implement Prophet baseline model and /forecast endpoint</name>
  <files>
    packages/analysis/models/baseline.py
    packages/analysis/routers/forecast.py
    packages/analysis/main.py
  </files>
  <action>
Implement the minimum code to make all tests pass:

**packages/analysis/models/baseline.py:**
- `fit_baseline(df: pd.DataFrame, user_events: list[dict] = None, forecast_days: int = 90) -> dict`
- Convert input to Prophet format: rename columns to 'ds' (date) and 'y' (revenue)
- Filter out zero-spend days (Pitfall 2): remove rows where spend_usd == 0 before fitting, but ONLY if zero-spend days exceed 20% of total rows. Otherwise keep them (some campaigns have legitimate zero-spend days like weekends).
- Load retail events from `data.retail_calendar.get_retail_events()` for the year range covering the data
- Merge user_events if provided
- Create `to_prophet_holidays()` DataFrame from combined events
- Configure Prophet: `seasonality_mode='multiplicative'` (revenue grows with trend per RESEARCH.md), `yearly_seasonality=True`, `weekly_seasonality=True`, `daily_seasonality=False`
- Fit model, generate future dataframe for forecast_days ahead
- Return dict with 'forecast' (list of {ds, yhat, yhat_lower, yhat_upper}) and 'components' (list of {ds, trend, yearly, weekly, holidays})
- Enforce minimum 30 data points after zero-spend filtering — raise ValueError if fewer

**packages/analysis/routers/forecast.py:**
- `router = APIRouter()`
- `@router.post("/forecast")` accepting ForecastRequest, returning ForecastResponse
- Convert request.metrics to DataFrame
- Call fit_baseline() with user_events from request
- Handle ValueError (insufficient data) -> return 400 with error message
- Handle Prophet fitting errors -> return 500 with diagnostic info
- Convert result to ForecastResponse Pydantic model

**packages/analysis/main.py:**
- Register forecast router: `app.include_router(forecast.router, prefix="/forecast", tags=["forecast"])`

Run tests until all pass. Then refactor: extract any helper functions, ensure docstrings on all public functions, add type hints.
  </action>
  <verify>
    <automated>cd C:/Users/brent/incremental-iq/packages/analysis && python -m pytest tests/test_forecast.py -x -v</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All forecast tests pass. Prophet model produces forecasts with confidence intervals. Holidays are injected. Zero-spend days are handled.</done>
</task>

</tasks>

<verification>
- All tests in test_forecast.py pass
- POST /forecast returns valid ForecastResponse with confidence intervals
- Holiday component visible in components output around retail event dates
- Zero-spend data does not crash the model
- Insufficient data (< 30 points) returns error response
</verification>

<success_criteria>
Prophet baseline forecasting works end-to-end: accepts campaign metrics, filters zero-spend days, injects retail calendar + user events as holidays, produces forecast with confidence intervals, returns decomposed components. STAT-01, STAT-03, STAT-07 (via Prophet's data-dependent fitting), and SEAS-01 (holiday calendar integration) are addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-statistical-engine/03-03-SUMMARY.md`
</output>
