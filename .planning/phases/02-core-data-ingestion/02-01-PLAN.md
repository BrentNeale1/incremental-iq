---
phase: 02-core-data-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/integrations.ts
  - packages/db/src/schema/sync-runs.ts
  - packages/db/src/schema/index.ts
  - packages/ingestion/package.json
  - packages/ingestion/tsconfig.json
  - packages/ingestion/src/index.ts
  - packages/ingestion/src/crypto.ts
  - packages/ingestion/src/types.ts
  - packages/ingestion/src/connector-base.ts
  - pnpm-workspace.yaml
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03, INTG-05]

must_haves:
  truths:
    - "integrations and sync_runs tables are defined in Drizzle schema with RLS policies matching Phase 1 pattern"
    - "packages/ingestion exists as a valid pnpm workspace package with all Phase 2 dependencies installed"
    - "OAuth tokens can be encrypted and decrypted using AES-256-GCM via a shared crypto module"
    - "A base connector type contract exists that all three platform connectors will implement"
  artifacts:
    - path: "packages/db/src/schema/integrations.ts"
      provides: "integrations table schema with RLS"
      contains: "pgTable('integrations'"
    - path: "packages/db/src/schema/sync-runs.ts"
      provides: "sync_runs table schema with RLS"
      contains: "pgTable('sync_runs'"
    - path: "packages/ingestion/package.json"
      provides: "Ingestion package with all Phase 2 dependencies"
      contains: "facebook-nodejs-business-sdk"
    - path: "packages/ingestion/src/crypto.ts"
      provides: "AES-256-GCM encrypt/decrypt for OAuth tokens"
      contains: "aes-256-gcm"
    - path: "packages/ingestion/src/types.ts"
      provides: "Shared TypeScript types for connectors, sync jobs"
      contains: "ConnectorConfig"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace definition including packages/* and apps/*"
      contains: "packages/*"
  key_links:
    - from: "packages/db/src/schema/index.ts"
      to: "packages/db/src/schema/integrations.ts"
      via: "re-export"
      pattern: "export.*from.*integrations"
    - from: "packages/db/src/schema/index.ts"
      to: "packages/db/src/schema/sync-runs.ts"
      via: "re-export"
      pattern: "export.*from.*sync-runs"
    - from: "packages/ingestion/src/crypto.ts"
      to: "process.env.TOKEN_ENCRYPTION_KEY"
      via: "environment variable"
      pattern: "TOKEN_ENCRYPTION_KEY"
---

<objective>
Add integrations and sync_runs tables to the database schema, scaffold the packages/ingestion package with all Phase 2 dependencies, and create shared infrastructure (token encryption, base types, connector contract).

Purpose: Every subsequent plan depends on these schema additions (to store OAuth credentials and sync history) and on the ingestion package (to house connector logic, normalizers, and scheduler). This plan is the foundation for the entire Phase 2 pipeline.

Output: Two new Drizzle schema files with RLS, a fully configured packages/ingestion workspace package, and shared utilities (crypto, types, connector base).
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-data-ingestion/02-RESEARCH.md
@.planning/phases/01-data-architecture/01-01-SUMMARY.md
@.planning/phases/01-data-architecture/01-02-SUMMARY.md
@packages/db/src/schema/index.ts
@packages/db/src/schema/roles.ts
@packages/db/src/schema/metrics.ts
@packages/db/src/schema/raw-pulls.ts
@packages/db/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integrations and sync_runs schema tables with RLS</name>
  <files>
    packages/db/src/schema/integrations.ts
    packages/db/src/schema/sync-runs.ts
    packages/db/src/schema/index.ts
  </files>
  <action>
Create two new Drizzle schema files following the exact Phase 1 RLS pattern (see metrics.ts, raw-pulls.ts for template).

**packages/db/src/schema/integrations.ts:**
Define `integrations` table matching the schema from 02-RESEARCH.md Required Schema Additions section. Key columns:
- `id` (uuid PK defaultRandom)
- `tenantId` (uuid, notNull)
- `platform` (text, notNull) — 'meta' | 'google_ads' | 'shopify'
- `status` (text, notNull) — 'connected' | 'error' | 'expired'
- `accountId` (text, nullable) — platform's account/store identifier
- `accountName` (text, nullable) — display name for UI
- `encryptedAccessToken` (text, nullable) — AES-256-GCM encrypted
- `encryptedRefreshToken` (text, nullable) — AES-256-GCM encrypted
- `tokenExpiresAt` (timestamp with timezone, nullable)
- `metadata` (jsonb, nullable) — platform-specific data (e.g., { loginCustomerId, adAccountId })
- `lastSyncedAt` (timestamp with timezone, nullable)
- `lastSyncStatus` (text, nullable) — 'success' | 'partial' | 'failed'
- `createdAt`, `updatedAt` (timestamps with timezone, defaultNow, notNull)

Add restrictive RLS policy using `appRole` and `current_setting('app.current_tenant_id')::uuid`, exactly matching the pattern in metrics.ts.

**packages/db/src/schema/sync-runs.ts:**
Define `syncRuns` table matching the schema from 02-RESEARCH.md Required Schema Additions section. Key columns:
- `id` (uuid PK defaultRandom)
- `tenantId` (uuid, notNull)
- `integrationId` (uuid, notNull, FK to integrations.id)
- `platform` (text, notNull)
- `runType` (text, notNull) — 'incremental' | 'backfill' | 'manual'
- `status` (text, notNull) — 'running' | 'success' | 'partial' | 'failed'
- `startedAt` (timestamp with timezone, defaultNow, notNull)
- `completedAt` (timestamp with timezone, nullable)
- `recordsIngested` (numeric(12,0), nullable)
- `errorMessage` (text, nullable)
- `progressMetadata` (jsonb, nullable) — { completed: 14, total: 36, unit: 'months' }

Add the same restrictive RLS policy.

**packages/db/src/schema/index.ts:**
Add re-exports for the two new modules (integrations, sync-runs) using bare imports (no .js extension — per Phase 1 decision).

Use bare imports (no `.js` extension) per Phase 1 decision — drizzle-kit bundler requires TypeScript resolution.
  </action>
  <verify>
    <automated>cd packages/db && npx tsc --noEmit --skipLibCheck</automated>
    <manual>integrations.ts and sync-runs.ts exist, index.ts re-exports both</manual>
  </verify>
  <done>integrations and sync_runs tables defined with full RLS, TypeScript compiles clean, index.ts re-exports both new schemas</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold packages/ingestion with dependencies and shared utilities</name>
  <files>
    pnpm-workspace.yaml
    packages/ingestion/package.json
    packages/ingestion/tsconfig.json
    packages/ingestion/src/index.ts
    packages/ingestion/src/crypto.ts
    packages/ingestion/src/types.ts
    packages/ingestion/src/connector-base.ts
  </files>
  <action>
**pnpm-workspace.yaml** (create at repo root):
```yaml
packages:
  - "packages/*"
  - "apps/*"
```

**packages/ingestion/package.json:**
- name: `@incremental-iq/ingestion`
- version: `0.0.1`
- type: `module`
- exports: `{ ".": "./src/index.ts" }`
- dependencies: `facebook-nodejs-business-sdk`, `google-ads-api`, `@shopify/shopify-api`, `bullmq`, `ioredis`, `p-retry`, `p-limit`, `date-fns`, `zod`, `@incremental-iq/db` (workspace:*)
- devDependencies: `@types/node`, `tsx`, `typescript`
- Use the versions from RESEARCH.md standard stack (facebook-nodejs-business-sdk latest, google-ads-api latest, @shopify/shopify-api latest, bullmq ^5.0.0, ioredis ^5.0.0, p-retry ^6.0.0, p-limit ^5.0.0, date-fns ^3.0.0, zod ^3.0.0)

**packages/ingestion/tsconfig.json:**
Match packages/db pattern: target ES2022, module ESNext, moduleResolution bundler, strict true, types ["node"], outDir dist, rootDir src.

**packages/ingestion/src/crypto.ts:**
Implement `encryptToken(plaintext: string): string` and `decryptToken(ciphertext: string): string` using Node.js built-in `crypto` module with AES-256-GCM (per RESEARCH.md Pattern 4). Read `TOKEN_ENCRYPTION_KEY` from `process.env`. Key must be 32 bytes hex-encoded. Use `randomBytes(12)` for IV. Concatenate iv + authTag + encrypted as base64 output. Export both functions.

**packages/ingestion/src/types.ts:**
Define shared types:
- `Platform = 'meta' | 'google_ads' | 'shopify'`
- `SyncType = 'incremental' | 'backfill' | 'manual'`
- `SyncStatus = 'running' | 'success' | 'partial' | 'failed'`
- `IntegrationStatus = 'connected' | 'error' | 'expired'`
- `ConnectorConfig = { tenantId: string; platform: Platform; integrationId: string; credentials: DecryptedCredentials }`
- `DecryptedCredentials = { accessToken: string; refreshToken?: string; metadata?: Record<string, unknown> }`
- `SyncJobData = { tenantId: string; platform: Platform; integrationId: string; type: SyncType; dateRange?: { start: string; end: string } }`
- `BackfillProgress = { completed: number; total: number; unit: string }`
- `NormalizedMetric` — a type matching the campaignMetrics insert shape from packages/db schema (date, tenantId, campaignId, source, spendUsd, impressions, clicks, ctr, cpm, directRevenue, directConversions, directRoas)

**packages/ingestion/src/connector-base.ts:**
Define an abstract base (or interface) `PlatformConnector`:
- `fetchCampaigns(config: ConnectorConfig): Promise<RawCampaignData[]>` — pulls campaign hierarchy
- `fetchMetrics(config: ConnectorConfig, dateRange: { start: string; end: string }): Promise<RawMetricData[]>` — pulls daily metrics for a date range
- `refreshTokenIfNeeded(config: ConnectorConfig): Promise<DecryptedCredentials>` — handles token refresh
- Also export `RawCampaignData` and `RawMetricData` as generic types that each platform connector will type-narrow

**packages/ingestion/src/index.ts:**
Re-export all modules: crypto, types, connector-base.

After creating files, run `pnpm install` from the repo root to link workspace packages.
  </action>
  <verify>
    <automated>cd packages/ingestion && npx tsc --noEmit --skipLibCheck</automated>
    <manual>pnpm install succeeds, packages/ingestion/node_modules exists, all dependencies resolved</manual>
  </verify>
  <done>packages/ingestion is a valid workspace package, TypeScript compiles, crypto module exports encrypt/decrypt, types and connector-base define the contract for all three platform connectors</done>
</task>

</tasks>

<verification>
1. `cd packages/db && npx tsc --noEmit --skipLibCheck` — zero errors
2. `cd packages/ingestion && npx tsc --noEmit --skipLibCheck` — zero errors
3. `packages/db/src/schema/integrations.ts` exists with pgTable('integrations') and RLS policy
4. `packages/db/src/schema/sync-runs.ts` exists with pgTable('sync_runs') and RLS policy
5. `packages/ingestion/src/crypto.ts` exports encryptToken and decryptToken
6. `pnpm-workspace.yaml` exists at repo root
</verification>

<success_criteria>
- Two new schema tables (integrations, sync_runs) with RLS policies matching Phase 1 pattern
- packages/ingestion scaffold with all Phase 2 dependencies installed
- AES-256-GCM token encryption utility ready for OAuth token storage
- Base connector types and contract defined for Meta, Google Ads, Shopify implementations
- TypeScript compiles clean in both packages/db and packages/ingestion
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-data-ingestion/02-01-SUMMARY.md`
</output>
