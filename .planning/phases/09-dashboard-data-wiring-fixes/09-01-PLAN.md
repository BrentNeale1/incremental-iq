---
phase: 09-dashboard-data-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/hooks/useCampaigns.ts
  - apps/web/app/(dashboard)/page.tsx
  - apps/web/components/layout/DashboardLayoutClient.tsx
  - apps/web/components/dashboard/KpiCard.tsx
  - apps/web/components/charts/PlatformComparisonChart.tsx
autonomous: true
requirements: [RPRT-01, RPRT-07]
gap_closure: true
must_haves:
  truths:
    - "Platform comparison chart in Executive Overview displays correct revenue values (not zeros)"
    - "Lead_gen tenants see Leads terminology instead of Revenue in KPI cards"
    - "Lead_gen tenants see Leads terminology in PlatformComparisonChart legend and tooltips"
    - "Ecommerce tenants still see Revenue terminology (no regression)"
  artifacts:
    - path: "apps/web/lib/hooks/useCampaigns.ts"
      provides: "CampaignRow interface aligned to API response"
      contains: "revenue: number"
    - path: "apps/web/app/(dashboard)/page.tsx"
      provides: "buildPlatformData using row.revenue"
      contains: "row.revenue"
    - path: "apps/web/components/layout/DashboardLayoutClient.tsx"
      provides: "useOutcomeMode called at layout level"
      contains: "useOutcomeMode"
    - path: "apps/web/components/dashboard/KpiCard.tsx"
      provides: "Dynamic METRIC_LABELS based on outcomeMode"
      contains: "outcomeMode"
    - path: "apps/web/components/charts/PlatformComparisonChart.tsx"
      provides: "Dynamic chart labels based on outcomeMode"
      contains: "outcomeMode"
  key_links:
    - from: "apps/web/lib/hooks/useCampaigns.ts"
      to: "apps/web/app/api/dashboard/campaigns/route.ts"
      via: "CampaignRow interface field names match API JSON keys"
      pattern: "revenue: number"
    - from: "apps/web/components/layout/DashboardLayoutClient.tsx"
      to: "apps/web/hooks/useOutcomeMode.ts"
      via: "useOutcomeMode tenantId call syncs outcomeMode to Zustand store"
      pattern: "useOutcomeMode"
    - from: "apps/web/components/dashboard/KpiCard.tsx"
      to: "apps/web/lib/store/dashboard.ts"
      via: "Reads outcomeMode from Zustand to select dynamic labels"
      pattern: "useDashboardStore.*outcomeMode"
---

<objective>
Fix two dashboard data wiring bugs: (1) CampaignRow type mismatch causing zero-revenue platform chart, and (2) orphaned useOutcomeMode hook preventing lead_gen terminology from appearing in dashboard views.

Purpose: The platform comparison chart shows zero revenue for all platforms because buildPlatformData reads `row.directRevenue` which does not exist in the API response. Lead_gen tenants see "Revenue" labels everywhere because useOutcomeMode is never called and KpiCard labels are hardcoded.

Output: Correct platform chart revenue bars, dynamic KPI/chart labels that respect tenant outcome mode.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard-data-wiring-fixes/09-RESEARCH.md

@apps/web/lib/hooks/useCampaigns.ts
@apps/web/app/(dashboard)/page.tsx
@apps/web/app/api/dashboard/campaigns/route.ts
@apps/web/components/layout/DashboardLayoutClient.tsx
@apps/web/components/dashboard/KpiCard.tsx
@apps/web/components/charts/PlatformComparisonChart.tsx
@apps/web/hooks/useOutcomeMode.ts
@apps/web/lib/store/dashboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CampaignRow type mismatch and buildPlatformData zero-revenue bug</name>
  <files>apps/web/lib/hooks/useCampaigns.ts, apps/web/app/(dashboard)/page.tsx</files>
  <action>
**Bug:** The API route `/api/dashboard/campaigns` returns `{ revenue }` but the hook's `CampaignRow` interface has `directRevenue` and `modeledRevenue`. The `buildPlatformData` function reads `row.directRevenue` which is always `undefined`, coerced to `0`.

**Step 1 — Fix CampaignRow in useCampaigns.ts:**
Replace the `CampaignRow` interface to match the actual API response fields. The API returns: `id, name, platform, funnelStage, spend, revenue, roas, liftMean, liftLower, liftUpper, confidence, status, isRollup`. Remove `directRevenue` and `modeledRevenue` fields. Add `revenue: number` instead. Confirm all other field names match by cross-referencing `apps/web/app/api/dashboard/campaigns/route.ts` response builder section.

**Step 2 — Fix buildPlatformData in page.tsx:**
Update `buildPlatformData` function:
1. Change parameter type to use `revenue: number` (not `directRevenue`)
2. Change the body to read `row.revenue` instead of `row.directRevenue`
3. For `incrementalRevenue`: derive it as `row.revenue * (row.liftMean ?? 0)` — this approximates incremental revenue using the lift fraction. Add a code comment: `// Approximate: incremental revenue = revenue * lift fraction (v1 approximation)`
4. Update the function's input type annotation to include `liftMean: number | null` alongside `platform`, `spend`, `revenue`
5. Update the call site where `buildPlatformData` is invoked to pass the correct fields from the campaigns data

**What NOT to do:**
- Do NOT add `incrementalRevenue` or `directRevenue` to the API response — that would be scope creep requiring DB changes
- Do NOT rename the API's `revenue` field — it is correct as-is
- Do NOT change any other part of page.tsx beyond `buildPlatformData` and its call site
  </action>
  <verify>
    <automated>cd C:/Users/brent/incremental-iq && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
    <manual>Confirm useCampaigns.ts no longer contains "directRevenue" or "modeledRevenue". Confirm page.tsx buildPlatformData reads row.revenue.</manual>
  </verify>
  <done>CampaignRow interface in useCampaigns.ts matches API response fields exactly (revenue, not directRevenue). buildPlatformData reads row.revenue and derives incrementalRevenue from revenue * liftMean. TypeScript compiles without errors related to these files.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useOutcomeMode hook and make KPI/chart labels dynamic</name>
  <files>apps/web/components/layout/DashboardLayoutClient.tsx, apps/web/components/dashboard/KpiCard.tsx, apps/web/components/charts/PlatformComparisonChart.tsx</files>
  <action>
**Bug:** `useOutcomeMode` hook exists at `apps/web/hooks/useOutcomeMode.ts` but is never called anywhere. The Zustand store's `outcomeMode` stays at default `'ecommerce'`, and all labels are hardcoded as "Revenue".

**Step 1 — Wire useOutcomeMode in DashboardLayoutClient.tsx:**
1. Add import: `import { useOutcomeMode } from '@/hooks/useOutcomeMode';`
2. Call `useOutcomeMode(tenantId);` right after the existing `useMarkets(tenantId);` call — this follows the exact same pattern (fetch at layout level, sync to Zustand, children read from store)
3. Do NOT change any other JSX or logic in DashboardLayoutClient

**Step 2 — Make KpiCard labels dynamic:**
1. In `KpiCard.tsx`, import `useDashboardStore` from `@/lib/store/dashboard` (if not already imported)
2. Inside the component function, read: `const outcomeMode = useDashboardStore((s) => s.outcomeMode);`
3. Move the `METRIC_LABELS` dict inside the component function body (if currently at module scope) and make it dynamic:
   - `revenue`: `outcomeMode === 'lead_gen' ? 'Leads' : 'Revenue'`
   - `roas`: `outcomeMode === 'lead_gen' ? 'Cost per Lead' : 'ROAS'`
   - `incremental_revenue`: `outcomeMode === 'lead_gen' ? 'Incremental Leads' : 'Incremental Revenue'`
   - `spend`, `lift_pct`, `avg_confidence`: keep unchanged
4. Do NOT change `formatKpiValue` — v1 keeps existing number formatting (the audit flagged terminology only, not formatting)

**Step 3 — Make PlatformComparisonChart labels dynamic:**
1. The `chartConfig` object is at module scope — move it inside the component function body
2. Read `outcomeMode` from Zustand: `const outcomeMode = useDashboardStore((s) => s.outcomeMode);`
3. Update labels:
   - `revenue.label`: `outcomeMode === 'lead_gen' ? 'Leads' : 'Revenue'`
   - `incrementalRevenue.label`: `outcomeMode === 'lead_gen' ? 'Incremental Leads' : 'Incremental Revenue'`
   - `spend.label`: keep as 'Spend'
4. If `chartConfig` is typed with `satisfies ChartConfig`, keep that pattern

**What NOT to do:**
- Do NOT prop-drill `terms` or `outcomeMode` through component hierarchies — read directly from Zustand store (consistent with how KpiGrid reads kpiOrder)
- Do NOT change `formatKpiValue` for ROAS — label change only for v1
- Do NOT modify the useOutcomeMode hook itself — it already works correctly
- Do NOT touch IncrementalRevenueChart unless it also has hardcoded "Revenue" labels (check first — if it does, apply same pattern)
  </action>
  <verify>
    <automated>cd C:/Users/brent/incremental-iq && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
    <manual>Confirm DashboardLayoutClient.tsx contains useOutcomeMode(tenantId) call. Confirm KpiCard.tsx reads outcomeMode from Zustand and uses it in METRIC_LABELS. Confirm PlatformComparisonChart.tsx has dynamic chartConfig labels.</manual>
  </verify>
  <done>useOutcomeMode is called in DashboardLayoutClient (populating Zustand store). KpiCard renders "Leads" / "Cost per Lead" / "Incremental Leads" for lead_gen tenants and "Revenue" / "ROAS" / "Incremental Revenue" for ecommerce tenants. PlatformComparisonChart legend and tooltips also show dynamic labels. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx tsc --noEmit --project apps/web/tsconfig.json` passes with no errors
2. No references to `directRevenue` remain in `useCampaigns.ts` or `page.tsx`
3. `useOutcomeMode` is imported and called in `DashboardLayoutClient.tsx`
4. `KpiCard.tsx` reads `outcomeMode` from Zustand and uses it to compute label strings
5. `PlatformComparisonChart.tsx` reads `outcomeMode` from Zustand and uses it in chartConfig labels
6. `grep -r "directRevenue" apps/web/lib/hooks/useCampaigns.ts apps/web/app/(dashboard)/page.tsx` returns no matches
7. `grep -r "useOutcomeMode" apps/web/components/layout/DashboardLayoutClient.tsx` returns a match
</verification>

<success_criteria>
- Platform comparison chart will display correct revenue values (row.revenue is a real number, not undefined)
- Lead_gen tenants will see "Leads", "Cost per Lead", "Incremental Leads" in KPI cards and chart legends
- Ecommerce tenants continue to see "Revenue", "ROAS", "Incremental Revenue" (no regression)
- TypeScript compiles cleanly across the web app
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-data-wiring-fixes/09-01-SUMMARY.md`
</output>
