---
phase: 10-dashboard-polish-and-integration-fixes
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/insights/DrillDownTable.tsx
  - apps/web/app/(dashboard)/insights/page.tsx
autonomous: true
gap_closure: true
requirements: [MRKT-04]
must_haves:
  truths:
    - "Switching market in AppHeader filters the DrillDownTable campaign list to only campaigns in that market"
    - "Switching back to All Markets shows all campaigns in the DrillDownTable"
    - "DrillDownTable re-fetches when market selection changes (no stale cache)"
  artifacts:
    - path: "apps/web/components/insights/DrillDownTable.tsx"
      provides: "marketId prop on DrillDownTableProps and useDrillData hook"
      contains: "marketId"
    - path: "apps/web/app/(dashboard)/insights/page.tsx"
      provides: "selectedMarket passed to DrillDownTable"
      contains: "marketId={selectedMarket"
  key_links:
    - from: "apps/web/app/(dashboard)/insights/page.tsx"
      to: "DrillDownTable"
      via: "marketId prop"
      pattern: "marketId=\\{selectedMarket"
    - from: "apps/web/components/insights/DrillDownTable.tsx"
      to: "/api/dashboard/campaigns"
      via: "URLSearchParams in useDrillData"
      pattern: "marketId.*params"
    - from: "apps/web/components/insights/DrillDownTable.tsx"
      to: "TanStack Query queryKey"
      via: "marketId in queryKey array"
      pattern: "queryKey.*marketId"
---

<objective>
Wire marketId from page-level Zustand state through DrillDownTable to the /api/dashboard/campaigns API route, so that selecting a market in AppHeader filters the campaign drill-down table.

Purpose: Close UAT Test 2 gap — "The stats in the top section update, but the campaigns dont." The stats cards already filter by market via useIncrementality (Plan 10-01), but DrillDownTable's useDrillData hook never received or forwarded the marketId parameter. The API route already supports marketId filtering (line 96 of campaigns/route.ts).

Output: DrillDownTable filters campaigns by selected market, matching the behavior of the stats cards above it.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard-polish-and-integration-fixes/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add marketId to DrillDownTable props and useDrillData hook, pass from page</name>
  <files>
    apps/web/components/insights/DrillDownTable.tsx
    apps/web/app/(dashboard)/insights/page.tsx
  </files>
  <action>
    In DrillDownTable.tsx:

    1. Add `marketId?: string` to `DrillDownTableProps` interface (line 161-164). This follows the exact same pattern as useIncrementality's marketId parameter added in Plan 10-01.

    2. Update `useDrillData` function signature (line 84) to accept a fourth parameter `marketId: string | undefined`:
       ```
       function useDrillData(
         dateRange: DateRange,
         platform: string | undefined,
         level: DrillLevel,
         marketId: string | undefined,
       )
       ```

    3. Add `marketId` to the queryKey array (line 93) so TanStack Query busts cache on market change:
       ```
       queryKey: ['drill-down', from, to, platform, level, marketId],
       ```

    4. Include marketId in URLSearchParams (line 95-100) — add it alongside the existing platform conditional spread:
       ```
       const params = new URLSearchParams({
         from,
         to,
         level,
         ...(platform && platform !== 'all' ? { platform } : {}),
         ...(marketId ? { marketId } : {}),
       });
       ```

    5. Update the `DrillDownTable` component function (line 181) to destructure `marketId` from props:
       ```
       export function DrillDownTable({ dateRange, onSelectRow, marketId }: DrillDownTableProps)
       ```

    6. Pass `marketId` to `useDrillData` call (line 192):
       ```
       const { data: rows, isLoading, isError } = useDrillData(dateRange, platformFilter !== 'all' ? platformFilter : undefined, level, marketId);
       ```

    In insights/page.tsx:

    7. Pass `selectedMarket` to `DrillDownTable` (line 275-276). The page already reads `selectedMarket` from Zustand (line 41). Add the marketId prop:
       ```
       <DrillDownTable
         dateRange={dateRange}
         marketId={selectedMarket ?? undefined}
         onSelectRow={(row) => {
           setSelectedRow(row);
           setSidebarOpen(true);
         }}
       />
       ```

    IMPORTANT: Do NOT modify the API route (campaigns/route.ts) — it already reads marketId from searchParams (line 96) and uses it for innerJoin filtering (lines 186-194).

    This follows the same pattern established in Plan 10-01 for useIncrementality:
    - marketId in queryKey prevents stale cache (same as useIncrementality queryKey pattern)
    - Conditional spread in URLSearchParams (same as useIncrementality fetch pattern)
    - Optional prop from page using `selectedMarket ?? undefined` (same as useIncrementality call in page.tsx line 52)
  </action>
  <verify>
    <automated>cd C:/Users/brent/incremental-iq && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
    <manual>Select a market in AppHeader dropdown. The DrillDownTable should update to show only campaigns in that market. Switch to a different market or back to All Markets — the table should immediately re-fetch and update.</manual>
  </verify>
  <done>DrillDownTable fetches campaigns filtered by marketId when a market is selected in AppHeader. Switching markets causes a cache-busted re-fetch. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit --project apps/web/tsconfig.json`
2. DrillDownTableProps interface includes `marketId?: string`
3. useDrillData queryKey includes marketId (grep for `'drill-down'.*marketId`)
4. useDrillData URLSearchParams includes marketId (grep for `marketId.*params`)
5. insights/page.tsx passes `marketId={selectedMarket` to DrillDownTable
6. campaigns/route.ts is NOT modified (already supports marketId)
</verification>

<success_criteria>
- UAT Test 2 passes: selecting a market filters both the stats cards AND the campaign drill-down table
- No TypeScript compilation errors
- API route unchanged — client-side wiring only
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-polish-and-integration-fixes/10-04-SUMMARY.md`
</output>
