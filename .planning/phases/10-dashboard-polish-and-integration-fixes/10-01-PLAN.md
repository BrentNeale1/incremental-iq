---
phase: 10-dashboard-polish-and-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/api/dashboard/incrementality/route.ts
  - apps/web/lib/hooks/useIncrementality.ts
  - apps/web/app/(dashboard)/insights/page.tsx
  - apps/web/app/(dashboard)/health/page.tsx
  - apps/web/app/(dashboard)/seasonality/page.tsx
autonomous: true
requirements: [MRKT-04, RPRT-05]

must_haves:
  truths:
    - "Insights page filters incrementality scores when a market is selected in AppHeader"
    - "Health page CSV export produces flat rows with no [object Object] cells"
    - "Seasonality page CSV export produces flat rows with no [object Object] cells"
    - "MethodologySidebar shows actual numeric dataPoints count instead of 'undefined'"
  artifacts:
    - path: "apps/web/app/api/dashboard/incrementality/route.ts"
      provides: "dataPoints field in API response"
      contains: "incrementalityScores.dataPoints"
    - path: "apps/web/lib/hooks/useIncrementality.ts"
      provides: "marketId parameter support"
      contains: "marketId"
    - path: "apps/web/app/(dashboard)/insights/page.tsx"
      provides: "Market-filtered insights page"
      contains: "selectedMarket"
    - path: "apps/web/app/(dashboard)/health/page.tsx"
      provides: "Flat export data for health page"
      contains: "flatRows"
    - path: "apps/web/app/(dashboard)/seasonality/page.tsx"
      provides: "Flat export data for seasonality page"
      contains: "flatRows"
  key_links:
    - from: "apps/web/app/(dashboard)/insights/page.tsx"
      to: "apps/web/lib/hooks/useIncrementality.ts"
      via: "selectedMarket passed as marketId param"
      pattern: "useIncrementality.*selectedMarket"
    - from: "apps/web/lib/hooks/useIncrementality.ts"
      to: "/api/dashboard/incrementality"
      via: "marketId query param in fetch URL"
      pattern: "marketId.*params"
    - from: "apps/web/app/(dashboard)/health/page.tsx"
      to: "apps/web/lib/export/excel.ts"
      via: "setExportData with flattened data"
      pattern: "setExportData.*flat"
---

<objective>
Fix dataPoints API gap, wire market filter to insights page, and flatten export data for health/seasonality pages.

Purpose: Close MRKT-04 (insights market segmentation) and RPRT-05 (export produces readable output) gaps. Also fix MethodologySidebar 'undefined' dataPoints bug.
Output: Working market-filtered insights page, flat CSV/Excel exports, correct dataPoints display.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard-polish-and-integration-fixes/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dataPoints to incrementality API and wire market filter to useIncrementality + insights page</name>
  <files>
    apps/web/app/api/dashboard/incrementality/route.ts
    apps/web/lib/hooks/useIncrementality.ts
    apps/web/app/(dashboard)/insights/page.tsx
  </files>
  <action>
**Part A — dataPoints in API route** (`apps/web/app/api/dashboard/incrementality/route.ts`):

1. Add `dataPoints: string | null` to the `RawScoreRow` and `RawScoreWithCampaign` interfaces.
2. Add `dataPoints: incrementalityScores.dataPoints` to BOTH select() calls (campaign detail mode and overview mode).
3. In the result mapping for both modes, add: `dataPoints: score.dataPoints ? parseInt(score.dataPoints, 10) : 0` — Drizzle numeric() returns string per project convention.

**Part B — marketId in useIncrementality** (`apps/web/lib/hooks/useIncrementality.ts`):

1. Add third parameter `marketId?: string` to `useIncrementality(campaignId?, scoreType, marketId?)`.
2. Add `marketId` to the queryKey array: `queryKey: ['incrementality', campaignId, scoreType, marketId]` — Pitfall 1: without this, TanStack Query caches stale result when market changes.
3. In the queryFn, add `...(marketId ? { marketId } : {})` to the URLSearchParams. The API route already handles `marketId` correctly (verified in RESEARCH.md).

**Part C — Market filter in insights page** (`apps/web/app/(dashboard)/insights/page.tsx`):

1. Import `useDashboardStore` from `@/lib/store/dashboard`.
2. Read `selectedMarket` and `markets` from Zustand store: `const selectedMarket = useDashboardStore((s) => s.selectedMarket)` and `const markets = useDashboardStore((s) => s.markets)`.
3. Pass `selectedMarket ?? undefined` as the third argument to `useIncrementality(undefined, 'adjusted', selectedMarket ?? undefined)`.
4. Add empty market state: when `selectedMarket` is set AND `scores` is empty/undefined AND `!scoresLoading`, render a centered message: "No incrementality data for [Market Name] yet" where `[Market Name]` comes from `markets.find(m => m.id === selectedMarket)?.displayName ?? 'this market'`. Use the same muted-foreground/text-center pattern used in other empty states in the project.
5. Ensure ALL sections on the page (ModelHealthOverview, charts, ProgressionView, DrillDownTable) receive the filtered scores data — they already consume `scores` from the hook, so passing `selectedMarket` through the hook is sufficient.

Per CONTEXT.md locked decisions: reuse the shared filter dropdown component (MarketSelector in AppHeader handles UI), everything filters (summary stats + charts + tables), filter state syncs with dashboard (selectedMarket from Zustand is already populated by DashboardLayoutClient useMarkets call).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
    <manual>Check that the incrementality API response includes dataPoints field. Check that insights page shows filtered data when a market is selected in AppHeader.</manual>
  </verify>
  <done>
    - MethodologySidebar shows numeric dataPoints value (not 'undefined')
    - useIncrementality accepts marketId parameter and includes it in queryKey and fetch URL
    - Insights page reads selectedMarket from Zustand and passes to useIncrementality
    - When market is selected with no data, insights page shows "No incrementality data for [Market Name] yet"
    - When market is selected with data, all insights sections (health overview, charts, progression, drill-down) show only that market's scores
  </done>
</task>

<task type="auto">
  <name>Task 2: Flatten health and seasonality page export data to eliminate [object Object] cells</name>
  <files>
    apps/web/app/(dashboard)/health/page.tsx
    apps/web/app/(dashboard)/seasonality/page.tsx
  </files>
  <action>
**Part A — Health page export** (`apps/web/app/(dashboard)/health/page.tsx`):

1. Find the `useEffect` or callback where `setExportData` is called with `syncHistory` data.
2. Replace the raw nested object with a flattened version. Map each `IntegrationSyncHistory` item to a flat record:
   ```
   platform: item.integration.platform
   status: item.integration.status
   freshness: item.integration.freshness
   last_sync_status: item.integration.lastSyncStatus ?? '—'
   is_stale: item.isStale ? 'Yes' : 'No'
   stale_since_hours: item.staleSinceHours != null ? item.staleSinceHours : '—'
   last_run_type: item.recentRuns[0]?.runType ?? '—'
   last_run_status: item.recentRuns[0]?.status ?? '—'
   records_ingested: item.recentRuns[0]?.recordsIngested != null ? item.recentRuns[0].recordsIngested : '—'
   ```
3. Pass the flattened array to `setExportData(flatRows, 'data-health')`.
4. Missing numeric values use em-dash '—' per CONTEXT locked decision.

**Part B — Seasonality page export** (`apps/web/app/(dashboard)/seasonality/page.tsx`):

1. Find the `useEffect` or callback where `setExportData` is called with seasonality data.
2. Flatten each `SeasonalEvent` to handle nulls. Map each item to:
   ```
   name: e.name
   event_date: e.eventDate
   weeks_until: e.weeksUntil
   days_until: e.daysUntil
   window_before_days: e.windowBefore ?? '—'
   window_after_days: e.windowAfter ?? '—'
   is_user_defined: e.isUserDefined ? 'Yes' : 'No'
   ```
3. Pass the flattened array to `setExportData(flatRows, 'seasonality-planning')`.
4. For historical performance data (if exported on this page): flatten any nested objects following the same pattern — one primitive value per cell. Use em-dash for null/undefined values.

Per CONTEXT locked decision: CSV is the priority format. Per RESEARCH Pitfall 6: export the actual data structure (event-oriented), do NOT fabricate monthly index columns that don't exist in the data model.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
    <manual>Open Health page, click Export CSV. Verify CSV contains flat columns (platform, status, freshness, etc.) with no [object Object]. Open Seasonality page, click Export CSV. Verify CSV contains flat columns (name, event_date, etc.) with no [object Object].</manual>
  </verify>
  <done>
    - Health page Export CSV produces flat rows: columns are platform, status, freshness, last_sync_status, is_stale, stale_since_hours, last_run_type, last_run_status, records_ingested
    - Seasonality page Export CSV produces flat rows: columns are name, event_date, weeks_until, days_until, window_before_days, window_after_days, is_user_defined
    - No cells contain [object Object] in any exported file
    - Missing/null values display as em-dash (—) in exports
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd apps/web && npx tsc --noEmit --skipLibCheck`
2. dataPoints visible in MethodologySidebar (not 'undefined')
3. Market filter on insights page: select a market in AppHeader, insights scores reflect only that market
4. Export health page: CSV has no [object Object] cells
5. Export seasonality page: CSV has no [object Object] cells
</verification>

<success_criteria>
- MRKT-04 closed for insights page: incrementality scores filtered by market selection
- RPRT-05 closed for health + seasonality: export produces flat, human-readable data
- MethodologySidebar dataPoints bug fixed (dataPoints fetched from DB and returned in API)
- All fixes compile without new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-polish-and-integration-fixes/10-01-SUMMARY.md`
</output>
