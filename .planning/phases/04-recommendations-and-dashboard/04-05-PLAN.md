---
phase: 04-recommendations-and-dashboard
plan: "05"
type: execute
wave: 3
depends_on:
  - "04-03"
  - "04-04"
files_modified:
  - apps/web/app/(dashboard)/insights/page.tsx
  - apps/web/app/(dashboard)/health/page.tsx
  - apps/web/components/insights/ModelHealthOverview.tsx
  - apps/web/components/insights/ConfidenceIntervalChart.tsx
  - apps/web/components/insights/ForecastActualChart.tsx
  - apps/web/components/insights/ProgressionView.tsx
  - apps/web/components/insights/MethodologySidebar.tsx
  - apps/web/components/insights/DrillDownTable.tsx
  - apps/web/components/health/SyncStatusList.tsx
  - apps/web/components/health/DataGapsTimeline.tsx
  - apps/web/components/health/IntegrationSettings.tsx
  - apps/web/lib/hooks/useIncrementality.ts
  - apps/web/lib/hooks/useSaturation.ts
  - apps/web/lib/hooks/useSyncHistory.ts
autonomous: true
requirements:
  - RPRT-03
  - RPRT-04

must_haves:
  truths:
    - "Statistical Insights page shows model accuracy trends and confidence interval visualizations"
    - "Methodology sidebar displays full model details (ITS type, Prophet parameters, Hill curve)"
    - "Drill-down table supports campaign -> cluster -> channel -> overall with expandable rows"
    - "Data Health page shows sync history per integration with status indicators"
    - "Stale data shows inline warning banners with reconnect links"
    - "Missing data gaps are visualized on a timeline"
  artifacts:
    - path: "apps/web/app/(dashboard)/insights/page.tsx"
      provides: "Statistical Insights page with model health, charts, methodology sidebar, drill-down"
    - path: "apps/web/app/(dashboard)/health/page.tsx"
      provides: "Data Health page with sync history, data gaps, integration settings"
    - path: "apps/web/components/insights/MethodologySidebar.tsx"
      provides: "Collapsible panel showing model methodology details"
      exports: ["MethodologySidebar"]
    - path: "apps/web/components/insights/DrillDownTable.tsx"
      provides: "Expandable table with preset and custom filters for hierarchy drill-down"
      exports: ["DrillDownTable"]
    - path: "apps/web/components/health/SyncStatusList.tsx"
      provides: "Per-integration sync status with freshness indicators"
      exports: ["SyncStatusList"]
  key_links:
    - from: "apps/web/components/insights/DrillDownTable.tsx"
      to: "apps/web/lib/hooks/useCampaigns.ts"
      via: "Fetches campaign data with level parameter for hierarchy"
      pattern: "useCampaigns.*level"
    - from: "apps/web/components/health/SyncStatusList.tsx"
      to: "apps/web/lib/hooks/useSyncHistory.ts"
      via: "Fetches sync_runs data for each integration"
      pattern: "useSyncHistory"
    - from: "apps/web/components/insights/ForecastActualChart.tsx"
      to: "apps/web/lib/hooks/useIncrementality.ts"
      via: "Fetches time series of scores for overlay chart"
      pattern: "useIncrementality"
---

<objective>
Build the Statistical Insights page (analyst-focused deep-dive with model health, confidence intervals, forecast vs actual overlays, methodology details, and drill-down tables) and the Data Health page (integration sync status, missing data gaps, and integration management).

Purpose: Statistical Insights is the analyst's home — full model transparency with expandable deep-dives (RPRT-03 drill-down, RPRT-07 analyst detail). Data Health provides integration monitoring and troubleshooting (RPRT-04 freshness detail).
Output: Two fully functional dashboard pages completing the 5-page navigation.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-recommendations-and-dashboard/04-RESEARCH.md
@.planning/phases/04-recommendations-and-dashboard/04-01-SUMMARY.md
@.planning/phases/04-recommendations-and-dashboard/04-02-SUMMARY.md
@.planning/phases/04-recommendations-and-dashboard/04-03-SUMMARY.md
@apps/web/components/layout/AppSidebar.tsx
@apps/web/lib/hooks/useCampaigns.ts
@apps/web/lib/hooks/useFreshness.ts
@packages/db/src/schema/sync-runs.ts
@packages/db/src/schema/integrations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Statistical Insights page with model health, charts, methodology sidebar, and drill-down table</name>
  <files>
    apps/web/app/(dashboard)/insights/page.tsx
    apps/web/components/insights/ModelHealthOverview.tsx
    apps/web/components/insights/ConfidenceIntervalChart.tsx
    apps/web/components/insights/ForecastActualChart.tsx
    apps/web/components/insights/ProgressionView.tsx
    apps/web/components/insights/MethodologySidebar.tsx
    apps/web/components/insights/DrillDownTable.tsx
    apps/web/lib/hooks/useIncrementality.ts
    apps/web/lib/hooks/useSaturation.ts
  </files>
  <action>
**A. Create useIncrementality hook (apps/web/lib/hooks/useIncrementality.ts):**
- TanStack Query hook fetching `/api/dashboard/incrementality?tenantId=X&scoreType=adjusted`
- Optional campaignId param for campaign-specific time series
- queryKey: ['incrementality', tenantId, campaignId, scoreType]
- staleTime: 5 * 60 * 1000

**B. Create useSaturation hook (apps/web/lib/hooks/useSaturation.ts):**
- TanStack Query hook fetching `/api/dashboard/saturation?tenantId=X`
- Optional campaignId for specific campaign curve
- queryKey: ['saturation', tenantId, campaignId]
- staleTime: 10 * 60 * 1000

**C. Create ModelHealthOverview (apps/web/components/insights/ModelHealthOverview.tsx):**
- 'use client' directive
- Top section showing aggregate model health metrics:
  - Average confidence across all campaigns
  - Number of campaigns scored / total campaigns
  - Model accuracy trend (average liftMean stability over last 4 scoring runs)
  - Last scoring run timestamp
- Uses shadcn Card grid layout — 4 summary cards
- Per design spec: "Model health and trends overview at top"

**D. Create ConfidenceIntervalChart (apps/web/components/insights/ConfidenceIntervalChart.tsx):**
- 'use client' directive
- Uses shadcn ChartContainer + Recharts AreaChart or custom ErrorBar component
- For each campaign (or selected campaign): shows liftMean line with shaded CI band (liftLower to liftUpper)
- X-axis: scored_at dates, Y-axis: lift value
- Gradient shading for the confidence band
- Tooltip shows exact liftMean, liftLower, liftUpper, confidence values

**E. Create ForecastActualChart (apps/web/components/insights/ForecastActualChart.tsx):**
- 'use client' directive
- Overlay chart showing forecast (predicted from Prophet baseline) vs actual (observed metrics)
- Two line series: forecast line (dashed) + actual line (solid)
- Divergence highlighted where actual significantly deviates from forecast
- Uses shadcn ChartContainer + Recharts LineChart with dual series

**F. Create ProgressionView (apps/web/components/insights/ProgressionView.tsx):**
- 'use client' directive
- Last 12 months performance progression
- Shows how model accuracy and confidence have improved over time
- Timeline with key milestones: first scoring run, first high-confidence recommendation, etc.
- Per design spec: "Long-range progression view — last 12 months performance progression, experiment history, model improvement over time"

**G. Create MethodologySidebar (apps/web/components/insights/MethodologySidebar.tsx):**
- 'use client' directive
- Uses shadcn Collapsible or Sheet component
- Persistent collapsible panel on the right side of the insights page
- Displays full model details for selected campaign:
  - ITS model type: "CausalPy Bayesian Interrupted Time Series"
  - Treatment window: date range of analysis
  - Prophet baseline: seasonality mode (multiplicative), holidays injected, changepoint prior
  - Hill saturation: alpha, mu, gamma parameters, current saturation %
  - Scoring metadata: scored_at, dataPoints, status
- Toggleable via button in header: "Show Methodology" / "Hide Methodology"
- Per design spec: "Methodology sidebar — persistent collapsible panel showing full model details"

**H. Create DrillDownTable (apps/web/components/insights/DrillDownTable.tsx):**
- 'use client' directive
- Similar to CampaignTable from Plan 04 but with expanded statistical columns
- Columns: Campaign/Group Name, Platform, Funnel Stage, Lift Mean, Lift Lower, Lift Upper, Confidence, Saturation %, Status, Data Points
- Expandable rows: click a cluster row to see its constituent campaigns
- Preset filters: "High Confidence" (>0.65), "Scale Candidates" (scale_up action), "Needs Data" (insufficient_data status)
- Custom filter: text search on campaign name, platform dropdown, confidence range slider
- Per design spec: "Expandable table rows with preset and custom filters for campaign -> cluster -> channel drill-down"
- Uses useCampaigns() hook with level parameter for hierarchy switching

**I. Wire Statistical Insights page (apps/web/app/(dashboard)/insights/page.tsx):**
- Layout: MethodologySidebar on right (collapsible), main content on left
- Section 1 — ModelHealthOverview (4 summary cards)
- Section 2 — ConfidenceIntervalChart + ForecastActualChart (side by side or stacked)
- Section 3 — ProgressionView (12-month timeline)
- Section 4 — DrillDownTable with filters
- Each section in a shadcn Collapsible wrapper per design spec: "Expandable deep-dive sections for each metric"
- Progressive loading with skeletons per section
- Page title: "Statistical Insights"
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
    <manual>Verify insights/page.tsx renders ModelHealthOverview + charts + MethodologySidebar + DrillDownTable; DrillDownTable supports level switching and has preset filters; MethodologySidebar shows model parameters</manual>
  </verify>
  <done>Statistical Insights page renders model health overview, confidence interval and forecast vs actual charts, 12-month progression view, collapsible methodology sidebar with full model details, and expandable drill-down table with preset/custom filters supporting campaign -> cluster -> channel hierarchy</done>
</task>

<task type="auto">
  <name>Task 2: Data Health page with sync status, data gaps, and integration settings</name>
  <files>
    apps/web/app/(dashboard)/health/page.tsx
    apps/web/components/health/SyncStatusList.tsx
    apps/web/components/health/DataGapsTimeline.tsx
    apps/web/components/health/IntegrationSettings.tsx
    apps/web/lib/hooks/useSyncHistory.ts
  </files>
  <action>
**A. Create useSyncHistory hook (apps/web/lib/hooks/useSyncHistory.ts):**
- TanStack Query hook that fetches integration status + sync run history
- GET `/api/integrations/status` for overall status (existing Phase 2 endpoint)
- For detailed history: query sync_runs table grouped by integration
- queryKey: ['syncHistory', tenantId]
- staleTime: 2 * 60 * 1000 (2 minutes — health data should be relatively fresh)

**B. Create SyncStatusList (apps/web/components/health/SyncStatusList.tsx):**
- 'use client' directive
- List of connected integrations with:
  - Platform icon (Meta, Google, Shopify)
  - Integration name/account
  - Last successful sync: relative time ("3 hours ago" via date-fns formatDistanceToNow)
  - Status badge: "Connected" (green), "Stale" (yellow, >24h), "Error" (red), "Disconnected" (gray)
  - Last 5 sync runs as mini timeline (success/fail dots)
- Stale integrations show inline warning banner per design spec:
  "Meta Ads data is 3 days stale — reconnect" with a link to `/api/oauth/meta`
- Per design spec: "Never hide the dashboard" — show last-known-good data with warning

**C. Create DataGapsTimeline (apps/web/components/health/DataGapsTimeline.tsx):**
- 'use client' directive
- Visual timeline showing data coverage over the last 90 days
- For each integration: colored bar showing days with data (green) vs days missing (red gaps)
- Uses a simple SVG or CSS grid visualization
- Helps user identify periods where data was not ingested
- Per design spec: "Missing data gaps over time"

**D. Create IntegrationSettings (apps/web/components/health/IntegrationSettings.tsx):**
- 'use client' directive
- Per-integration settings card:
  - Reconnect button: links to OAuth re-authorization flow (`/api/oauth/{platform}`)
  - Sync frequency display (shows "Daily at 2:00 AM UTC" — from BullMQ scheduler config)
  - Manual sync trigger button: POST to `/api/integrations/{id}/sync` (existing Phase 2 endpoint)
  - Disconnect button (future — placeholder for Phase 6 auth)
- Advanced settings section (collapsible):
  - Historical data range loaded
  - Data retention policy (informational)
- Per design spec: "Advanced integration settings" and "Direct links to reconnect/fix broken integrations"

**E. Wire Data Health page (apps/web/app/(dashboard)/health/page.tsx):**
- Section 1 — SyncStatusList (all integrations with status)
- Section 2 — DataGapsTimeline (visual coverage)
- Section 3 — IntegrationSettings (per-integration management)
- Progressive loading with skeletons
- Page title: "Data Health"
- Empty state: "No integrations connected yet — connect your first platform to start syncing data" with links to OAuth setup
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
    <manual>Verify health/page.tsx renders SyncStatusList + DataGapsTimeline + IntegrationSettings; SyncStatusList shows stale data warnings with reconnect links; IntegrationSettings has reconnect and manual sync buttons</manual>
  </verify>
  <done>Data Health page renders sync status per integration with freshness indicators and stale data warnings (RPRT-04), visual data gap timeline over 90 days, and integration settings with reconnect links and manual sync triggers</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --skipLibCheck` passes
2. /insights page renders ModelHealthOverview, charts, MethodologySidebar, DrillDownTable
3. DrillDownTable supports campaign/cluster/channel/overall drill-down (RPRT-03)
4. MethodologySidebar shows ITS model type, Prophet params, Hill curve params
5. /health page renders SyncStatusList with freshness badges (RPRT-04)
6. Stale integrations show inline warning with reconnect link
7. DataGapsTimeline visualizes data coverage
8. IntegrationSettings provides reconnect and manual sync
9. Both pages use progressive loading with skeletons
</verification>

<success_criteria>
- Statistical Insights provides deep-dive into model outputs for analysts
- Multi-level drill-down table works across all hierarchy levels (RPRT-03)
- Methodology sidebar shows full model transparency
- Data Health shows per-integration freshness with full detail (RPRT-04)
- Stale data gets warning banners, never hidden dashboard
- All 5 dashboard pages now navigable via sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/04-recommendations-and-dashboard/04-05-SUMMARY.md`
</output>
