---
phase: 04-recommendations-and-dashboard
plan: "04"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - apps/web/app/(dashboard)/performance/page.tsx
  - apps/web/app/(dashboard)/seasonality/page.tsx
  - apps/web/components/performance/PriorityQueue.tsx
  - apps/web/components/performance/PriorityItem.tsx
  - apps/web/components/performance/PlatformTabs.tsx
  - apps/web/components/performance/PlatformOverview.tsx
  - apps/web/components/performance/CampaignTable.tsx
  - apps/web/components/seasonality/SeasonalTimeline.tsx
  - apps/web/components/seasonality/HistoricalComparison.tsx
  - apps/web/components/seasonality/EventCard.tsx
  - apps/web/lib/hooks/useSeasonality.ts
autonomous: true
requirements:
  - RECC-05
  - RPRT-03

must_haves:
  truths:
    - "Marketing Performance page shows a priority queue of urgent campaign actions at the top"
    - "Platform tabs (Meta, Google, All) filter the performance view by ad platform"
    - "Campaign table supports drill-down from campaign to cluster to channel levels"
    - "Seasonality Planning page shows a forward-looking calendar timeline with upcoming retail events"
    - "Each seasonal event card includes budget recommendation based on historical data"
    - "Historical comparison section shows last year's performance vs this year's forecast"
  artifacts:
    - path: "apps/web/app/(dashboard)/performance/page.tsx"
      provides: "Marketing Performance page with priority queue and platform tabs"
    - path: "apps/web/app/(dashboard)/seasonality/page.tsx"
      provides: "Seasonality Planning page with timeline and historical comparison"
    - path: "apps/web/components/performance/PriorityQueue.tsx"
      provides: "Ranked list of urgent campaign actions (scale, watch, investigate)"
      exports: ["PriorityQueue"]
    - path: "apps/web/components/performance/CampaignTable.tsx"
      provides: "Multi-level campaign drill-down table"
      exports: ["CampaignTable"]
    - path: "apps/web/components/seasonality/SeasonalTimeline.tsx"
      provides: "Forward-looking visual timeline of retail events"
      exports: ["SeasonalTimeline"]
  key_links:
    - from: "apps/web/components/performance/PriorityQueue.tsx"
      to: "apps/web/lib/hooks/useRecommendations.ts"
      via: "Reads recommendations sorted by urgency/impact"
      pattern: "useRecommendations"
    - from: "apps/web/components/performance/CampaignTable.tsx"
      to: "apps/web/lib/hooks/useCampaigns.ts"
      via: "Fetches campaigns with level param for drill-down"
      pattern: "useCampaigns.*level"
    - from: "apps/web/components/seasonality/SeasonalTimeline.tsx"
      to: "apps/web/lib/hooks/useSeasonality.ts"
      via: "Fetches upcoming seasonal events and historical data"
      pattern: "useSeasonality"
---

<objective>
Build the Marketing Performance page (action-oriented campaign management with priority queue and platform tabs) and the Seasonality Planning page (forward-looking seasonal preparation with calendar timeline and historical comparison).

Purpose: Marketing Performance gives marketers their "to-do list" — campaigns ranked by urgency with platform-specific views. Seasonality Planning enables proactive budget adjustments ahead of known retail events, using historical data to set expectations (RECC-05).
Output: Two fully functional dashboard pages consuming API data from Plan 02.
</objective>

<execution_context>
@C:/Users/brent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-recommendations-and-dashboard/04-RESEARCH.md
@.planning/phases/04-recommendations-and-dashboard/04-01-SUMMARY.md
@.planning/phases/04-recommendations-and-dashboard/04-02-SUMMARY.md
@.planning/phases/04-recommendations-and-dashboard/04-03-SUMMARY.md
@apps/web/lib/hooks/useRecommendations.ts
@apps/web/lib/hooks/useCampaigns.ts
@apps/web/components/dashboard/DateRangePicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Marketing Performance page with priority queue, platform tabs, and campaign table</name>
  <files>
    apps/web/app/(dashboard)/performance/page.tsx
    apps/web/components/performance/PriorityQueue.tsx
    apps/web/components/performance/PriorityItem.tsx
    apps/web/components/performance/PlatformTabs.tsx
    apps/web/components/performance/PlatformOverview.tsx
    apps/web/components/performance/CampaignTable.tsx
  </files>
  <action>
**A. Create PriorityQueue (apps/web/components/performance/PriorityQueue.tsx):**
- 'use client' directive
- Consumes recommendations from useRecommendations() hook
- Displays campaigns ranked by urgency: scale_up (green), watch (yellow), investigate (red)
- Each item is a PriorityItem component
- Per user decision: "Priority queue at top — urgent campaign actions ranked: scale, watch, investigate"
- Max 10 items visible, "Show all" expander if more

**B. Create PriorityItem (apps/web/components/performance/PriorityItem.tsx):**
- Single campaign action card with:
  - Left color indicator (green/yellow/red based on action type)
  - Campaign name + platform badge (Meta/Google)
  - One-line action summary: "Scale up 25% — expected +$12K" or "Watch — confidence building" or "Investigate — insufficient data"
  - Right arrow linking to campaign detail (future enhancement — placeholder for now)
- Compact card layout — stacks vertically

**C. Create PlatformTabs (apps/web/components/performance/PlatformTabs.tsx):**
- 'use client' directive
- Uses shadcn Tabs component
- Three tabs: "All Platforms", "Meta Ads", "Google Ads"
- Selecting a tab filters the data shown in PlatformOverview and CampaignTable below
- Tab maintains state locally (URL param or component state)

**D. Create PlatformOverview (apps/web/components/performance/PlatformOverview.tsx):**
- Summary stats for selected platform: total spend, total revenue, avg ROAS, avg lift, campaign count
- Uses shadcn Card layout — 4-5 metric cards in a row
- Data filtered by selected platform from useCampaigns() with platform param

**E. Create CampaignTable (apps/web/components/performance/CampaignTable.tsx):**
- 'use client' directive
- Uses shadcn Table component
- Columns: Campaign Name, Platform, Funnel Stage, Spend, Revenue, ROAS, Lift Mean, Confidence, Status, Action
- Supports multi-level drill-down (RPRT-03):
  - Default: campaign level (individual campaigns)
  - "Cluster" button: groups by platform_funnelStage (e.g., meta_awareness)
  - "Channel" button: groups by platform (e.g., meta)
  - "Overall" button: single aggregate row
- Uses useCampaigns(dateRange, platform, level) hook with level parameter to fetch appropriate data
- Expandable rows: click a cluster row to see its constituent campaigns
- Sort by any column (client-side sort on current data)
- Per user decision: "expandable table rows with preset and custom filters for campaign -> cluster -> channel drill-down"
- Comfortable table spacing per design spec: generous row height, clear visual separation, hover highlights

**F. Wire Marketing Performance page (apps/web/app/(dashboard)/performance/page.tsx):**
- Section 1 — PriorityQueue at top
- Section 2 — PlatformTabs wrapping PlatformOverview + CampaignTable
- Progressive loading: skeleton for priority queue, then platform overview, then table
- Page title: "Marketing Performance"
- Design spec: practical, action-oriented — less stats, more "here's what to do"
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
    <manual>Verify performance/page.tsx renders PriorityQueue + PlatformTabs + CampaignTable; CampaignTable supports level switching; PlatformTabs filter by Meta/Google/All</manual>
  </verify>
  <done>Marketing Performance page renders priority queue with ranked campaign actions, platform tabs filtering by Meta/Google/All, platform overview metrics, and a multi-level campaign drill-down table supporting campaign/cluster/channel/overall views</done>
</task>

<task type="auto">
  <name>Task 2: Seasonality Planning page with timeline, event cards, and historical comparison</name>
  <files>
    apps/web/app/(dashboard)/seasonality/page.tsx
    apps/web/components/seasonality/SeasonalTimeline.tsx
    apps/web/components/seasonality/HistoricalComparison.tsx
    apps/web/components/seasonality/EventCard.tsx
    apps/web/lib/hooks/useSeasonality.ts
  </files>
  <action>
**A. Create useSeasonality hook (apps/web/lib/hooks/useSeasonality.ts):**
- TanStack Query hook that fetches `/api/dashboard/seasonality?tenantId=X&months=6`
- Returns: `{ upcoming: SeasonalEvent[], historical: HistoricalPerformance[] }`
- queryKey: ['seasonality', tenantId, months]
- staleTime: 30 * 60 * 1000 (30 minutes — seasonal data changes rarely)

**B. Create SeasonalTimeline (apps/web/components/seasonality/SeasonalTimeline.tsx):**
- 'use client' directive
- Visual horizontal timeline showing upcoming 6 months
- Events plotted on timeline as colored markers
- Each event shows:
  - Event name (e.g., "Black Friday/Cyber Monday")
  - Date or date range
  - Weeks until event
- Uses a horizontal scroll container for the timeline
- Events closer in time appear larger/more prominent
- Per user decision: "Calendar timeline (forward-looking) — visual timeline showing upcoming retail events with budget recommendations per campaign"

**C. Create EventCard (apps/web/components/seasonality/EventCard.tsx):**
- Individual event detail card (rendered below timeline or in expanded view)
- Shows: event name, date, weeks until, recommended action
- Budget recommendation per campaign: "BFCM: ramp Campaign X 3 weeks before, peak spend during, taper after"
- Per user decision format: "BFCM in 6 weeks: Campaign X scaled +40% last year, consider ramping now"
- Uses seasonal alert data from the recommendations API + seasonality API
- Color-coded by proximity: red (< 2 weeks), yellow (2-4 weeks), green (> 4 weeks)

**D. Create HistoricalComparison (apps/web/components/seasonality/HistoricalComparison.tsx):**
- 'use client' directive
- For each upcoming event, shows side-by-side comparison:
  - Last year: spend, revenue, lift, ROAS during event period
  - This year forecast: projected metrics based on current trend + model output
- Uses shadcn Card + Table layout
- Per user decision: "Last BFCM you spent $X and got Y incremental revenue, this year we project Z"
- If no historical data (first year): shows "No prior data — first {event} analysis will be available next year"

**E. Wire Seasonality Planning page (apps/web/app/(dashboard)/seasonality/page.tsx):**
- Section 1 — SeasonalTimeline (forward-looking calendar) at top
- Section 2 — EventCard grid for upcoming events (next 6 weeks highlighted)
- Section 3 — HistoricalComparison table
- Progressive loading with skeletons
- Page title: "Seasonality Planning"
- Empty state: "Seasonal planning activates 6 weeks before your first retail event" with calendar icon
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
    <manual>Verify seasonality/page.tsx renders SeasonalTimeline + EventCards + HistoricalComparison; EventCard shows budget recommendation format; HistoricalComparison shows last year vs this year</manual>
  </verify>
  <done>Seasonality Planning page renders a forward-looking calendar timeline with upcoming retail events, budget recommendation cards per campaign with historical context, and year-over-year comparison showing last year's performance vs current forecast (RECC-05)</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --skipLibCheck` passes
2. /performance page has PriorityQueue at top with ranked campaign actions
3. PlatformTabs filter between All/Meta/Google
4. CampaignTable supports level switching (campaign/cluster/channel/overall) for RPRT-03
5. /seasonality page has SeasonalTimeline with forward-looking events
6. EventCard shows budget recommendations in the specified format (RECC-05)
7. HistoricalComparison shows year-over-year data
8. Both pages use progressive loading with skeleton placeholders
</verification>

<success_criteria>
- Marketing Performance shows priority-ranked campaign actions
- Platform tabs filter data correctly
- Multi-level drill-down works (campaign -> cluster -> channel -> overall) (RPRT-03)
- Seasonality page proactively suggests budget adjustments ahead of seasonal periods (RECC-05)
- Historical comparison provides context for seasonal recommendations
- Both pages respect shared date range from Zustand store
</success_criteria>

<output>
After completion, create `.planning/phases/04-recommendations-and-dashboard/04-04-SUMMARY.md`
</output>
